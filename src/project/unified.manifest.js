import {
  SCHEMA_VERSION,
  stateSchema,
  actionSchema,
  mutationMatrix
} from "./project.manifest.js";

export const manifest = {
  SCHEMA_VERSION,
  stateSchema,
  actionSchema,
  mutationMatrix
};

